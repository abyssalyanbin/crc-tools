{
  "name": "@crc/presets",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.json",
  "files": ["dist"],
  "scripts": {
    "prebuild": "node ./tools/validate.mjs",
    "build": "mkdir -p dist && cp src/*.json dist/",
    "test": "node ./tools/validate.mjs",
    "lint": "echo skip",
    "typecheck": "echo skip"
  },
  "devDependencies": {
    "@crc/core": "workspace:*"
  }
}
